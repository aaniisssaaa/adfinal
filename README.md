# Rental Project 🚗

Полнофункциональное веб-приложение для аренды автомобилей с Node.js, Express и MongoDB.

## Описание

**Rental Project** — это система управления арендой транспортных средств с современным стеком технологий:
- **Backend**: Node.js + Express
- **База данных**: MongoDB
- **Frontend**: HTML5, CSS, JavaScript
- **API**: RESTful endpoints с CORS поддержкой

Приложение позволяет пользователям искать, бронировать и оставлять отзывы об автомобилях.

## Требования

- **Node.js** (v14+)
- **MongoDB** (локально запущенный на `mongodb://localhost:27017`)
- **npm** или **yarn**

## Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/aaniisssaaa/adfinal.git
cd rental-project
```

### 2. Установить зависимости
```bash
npm install
```

Будут установлены:
- `express` — веб-фреймворк
- `mongoose` — ODM для MongoDB
- `cors` — поддержка кроссдоменных запросов

### 3. Запустить MongoDB
```bash
# Windows (если установлен MongoDB)
mongod

# Linux/macOS
brew services start mongodb-community
# или
sudo systemctl start mongod
```

### 4. Запустить сервер
```bash
npm start
# или
node server.js
```

Сервер запустится на **http://localhost:3000**

## Структура проекта

```
rental-project/
├── server.js              # Основной файл приложения
├── package.json           # Зависимости и информация о проекте
├── package-lock.json      # Фиксированные версии зависимостей
├── public/                # Статические файлы (фронтенд)
│   ├── index.html         # Главная страница
│   ├── login.html         # Страница входа
│   ├── search.html        # Поиск автомобилей
│   ├── dashboard.html     # Личный кабинет
│   └── manage.html        # Управление автомобилями
├── .vscode/               # Конфигурация VS Code
│   └── launch.json        # Конфигурация отладки
└── README.md              # Этот файл
```

## API Endpoints

### 🔐 Аутентификация
#### `POST /api/login`
Вход в систему

**Request:**
```json
{
  "username": "admin",
  "password": "12345"
}
```

**Response:**
```json
{ "success": true }
```

---

### 🔍 Поиск автомобилей
#### `GET /api/search?brand=Toyota&maxPrice=100`
Поиск автомобилей по марке и цене

**Query параметры:**
- `brand` — марка автомобиля (опционально)
- `maxPrice` — максимальная цена в день (опционально)

**Response:**
```json
[
  {
    "_id": "507f1f77bcf86cd799439011",
    "brand": "Toyota",
    "model": "Camry",
    "pricePerDay": 80,
    "status": "available",
    "reviews": []
  }
]
```

---

### 📊 Статистика
#### `GET /api/stats`
Получить статистику по марках автомобилей (агрегация)

**Response:**
```json
[
  {
    "_id": "Toyota",
    "totalCars": 5,
    "avgPrice": 85.5
  },
  {
    "_id": "BMW",
    "totalCars": 3,
    "avgPrice": 120
  }
]
```

---

### 🚗 CRUD операции с автомобилями

#### `GET /api/vehicles`
Получить все автомобили

---

#### `POST /api/vehicles`
Добавить новый автомобиль

**Request:**
```json
{
  "brand": "Tesla",
  "model": "Model 3",
  "pricePerDay": 150,
  "status": "available"
}
```

---

#### `DELETE /api/vehicles/:id`
Удалить автомобиль по ID

---

#### `POST /api/vehicles/:id/reviews`
Добавить отзыв к автомобилю

**Request:**
```json
{
  "user": "Иван Петров",
  "comment": "Отличный автомобиль!"
}
```

## Особенности реализации

### 📑 Индексирование
Схема `vehicleSchema` использует составной индекс:
```javascript
vehicleSchema.index({ brand: 1, pricePerDay: -1 });
```
Это ускоряет поиск по марке и сортировку по цене.

### 🔀 Агрегация (Pipeline)
Эндпоинт `/api/stats` использует MongoDB Aggregation Pipeline для группировки и подсчета статистики.

### 📤 CORS
Приложение поддерживает кроссдоменные запросы для фронтенда на других доменах.

## Примеры использования

### Поиск автомобилей (cURL)
```bash
curl "http://localhost:3000/api/search?brand=Toyota&maxPrice=100"
```

### Добавление отзыва (JavaScript)
```javascript
fetch('http://localhost:3000/api/vehicles/507f1f77bcf86cd799439011/reviews', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    user: 'Мария',
    comment: 'Рекомендую!'
  })
})
.then(r => r.json())
.then(data => console.log(data))
```